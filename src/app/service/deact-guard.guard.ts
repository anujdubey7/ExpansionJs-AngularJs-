import { CanDeactivateFn } from '@angular/router';

// {} [] *

export const deactGuardGuard: CanDeactivateFn<unknown> = (component, currentRoute, currentState, nextState) => {

  
  return true;
};
